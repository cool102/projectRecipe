import java.time.LocalDate;
import java.time.LocalDateTime;
import java.util.Scanner;

class Main {
    public static void main(String[] args) {
        // put your code here
        //считать строку
        //изъять дату
        //изъять количество дней

        // получить эталонную дату в этом году
         //взять год
            //- уточнить количество дней в году
               // -прибавить в к дате год-мес-день количество дней в году

        //прибавит к дате кол-во дней

        //сравнить получившуюся дату с эталоном

      // Scanner scanner = new Scanner(System.in);
      // String[] arr =  scanner.nextLine().split("\\s");
      // LocalDate inputDate = LocalDate.parse(arr[0]);
      // long inputDays = Long.parseLong(arr[1]);
      // long len = inputDate.lengthOfYear();

      // LocalDate startDate = LocalDate.of(inputDate.getYear(),1,1);
      // LocalDate etalon = startDate.plusDays(len);

      // String res = inputDate.plusDays(inputDays).isEqual(etalon)? "true": "false";
      // System.out.println(res);





    }
}